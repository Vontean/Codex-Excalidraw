interface:
  display_name: "Excalidraw Diagram"
  short_description: "Create and iterate editable Excalidraw canvases"
  default_prompt: "Use $excalidraw-diagram to turn this idea into an editable Excalidraw canvas and iterate from the current canvas state."

policy:
  allow_implicit_invocation: true
